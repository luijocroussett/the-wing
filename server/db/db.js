// in-memory object for saving all current games
// Best practice would have been to create an interface so the data store is loosely coupled from the actions
const gamesObj = {};

module.exports = gamesObj;